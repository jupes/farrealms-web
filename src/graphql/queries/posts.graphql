query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    hasMorePosts
    posts {
      _id
      title
      text
      textSnippet
      score
      authorId
      createdAt
      updatedAt
    }
    
  }
}
